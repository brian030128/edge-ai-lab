{
  "$comment": "Where the page reads its content from. Fill in repo.owner and repo.name to turn on the footer's edit link and the ?source=github preview mode; set source.mode to 'github' to read content straight from the repository at runtime.",
  "source": {
    "mode": "local",
    "local": {
      "base": "content"
    },
    "github": {
      "path": "content"
    }
  },
  "repo": {
    "host": "github.com",
    "owner": "brian030128",
    "name": "edge-ai-lab",
    "branch": "main"
  },
  "cache": {
    "ttlSeconds": 300
  }
}
